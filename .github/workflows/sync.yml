name: Sync Scryfall + MTGJson

on:
  schedule:
    - cron: '0 15 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - uses: actions/setup-node@v4
        with:
          node-version: '18'

      - run: npm install

      - name: Install Git LFS
        run: |
          git lfs install
          git lfs pull

      - run: NODE_OPTIONS="--max-old-space-size=4096" npm run sync

      - name: Commit and push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git lfs migrate import --include="docs/*.json.gz" || true
          git add docs/*.json.gz docs/manifest.json
          git commit -m "Auto-deploy: $(date)" || echo "No changes"
          git push
