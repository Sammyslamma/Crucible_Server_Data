{
  "name": "crucible-server-data",
  "version": "1.0.0",
  "type": "module",
  "description": "Scryfall + MTGJson + Card Kingdom merge pipeline for MTG collection app",
  "main": "scripts/sync-scryfall.js",
  "scripts": {
    "sync": "node scripts/sync-scryfall.js",
    "watchdog": "node scripts/watchdog.mjs"
  },
  "dependencies": {
    "node-fetch": "^3.3.0",
    "JSONStream": "^1.3.5"
  }
}